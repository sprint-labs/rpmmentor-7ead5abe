# Legacy migration archive

These 41 files are preserved byte-for-byte from the pre-reconciliation
repository history. They are retained for investigation and provenance only.

Do **not** copy or execute this directory as a migration chain. Its versions and
contents do not match the authoritative production ledger, and replaying it can
reintroduce resolved schema and security drift.

The deployable chain lives only in `supabase/migrations/`.
